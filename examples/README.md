# Hermes Kanban MCP examples

## opencode-mcp-config.json

Replace `<TOKEN>` with the actual hermes-kanban bearer token. Place this file at
`~/.config/opencode/opencode.json` (merge the `mcp` key) or include it as a
project-level `.opencode/opencode.jsonc`.
